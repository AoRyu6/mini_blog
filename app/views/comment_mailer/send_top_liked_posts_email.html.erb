<h1 class="text-center mb-5 display-4">🔥 いいねランキング [TOP 10]</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <ul class="list-group list-group-flush" style="list-style: none;">
        <% @posts.each_with_index do |post, index| %>
          <li class="list-group-item border rounded shadow-sm mb-3 p-3" style="background-color: #f8f9fa;">
            <div class="d-flex justify-content-between align-items-center">
              <div>
                <h5 class="mb-0">
                  <span class="badge bg-primary" style="font-size: 1.2rem;"><%= "#{index + 1}位" %></span>
                  <a href="<%= post_url(post) %>" class="text-decoration-none text-dark fw-bold">
                    <%= post.content %>
                  </a>
                </h5>
                <small class="text-muted">(<%= post.likes.count %> いいね)</small>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>